@model IEnumerable<HospitalAppointmentSystem.Models.Saatler>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.SaatDilimi)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Secilebilir)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CalismaGun)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SaatDilimi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Secilebilir)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CalismaGun.CalismaGunId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SaatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SaatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SaatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
